---
- name: Run preflight assertions
  ansible.builtin.include_tasks: preflight.yml
  tags: always

- name: Release, distribution, family
  ansible.builtin.debug:
    msg: "distribution_release:{{ ansible_distribution_release }} distribution:{{ ansible_distribution }} os_family:{{ ansible_os_family }} release:{{ ansible_lsb.major_release }}"
  tags: backuppc_client

- name: Gather OS specific variables
  ansible.builtin.include_vars: "{{ lookup('first_found', params) }}"
  vars:
    params:
      files:
        - "{{ ansible_distribution_release }}.yml"
        - "{{ ansible_distribution }}.yml"
        - "{{ ansible_os_family }}.yml"
        - "default.yml"
      paths:
        - "vars"
  tags: backuppc_client

- name: Import tasks debian.yml
  ansible.builtin.include_tasks: debian.yml
  when: ansible_os_family == 'Debian'
  tags: backuppc_client

- name: Import tasks ubuntu.yml
  ansible.builtin.import_tasks: ubuntu.yml
  when: ansible_distribution == 'Ubuntu'
  tags: backuppc_client

- name: Import tasks redhat.yml
  ansible.builtin.include_tasks: redhat.yml
  when: ansible_os_family == 'RedHat'
  tags: backuppc_client

# Tasks after this comment (are hopefully) common to a linux bases operating
# systems?
#
- name: Delete old backuppc users
  ansible.builtin.user:
    name: "{{ backuppc_client_item.username }}"
    state: "absent"
    remove: true
  loop: "{{ deprecated_backuppc_username }}"
  loop_control:
    loop_var: backuppc_client_item
  tags: backuppc_client

# Look in group_vars and host_vars for default value for {{ backuppc_client }}
#
- name: Create backuppc user
  ansible.builtin.user:
    name: "{{ backuppc_client_item.username }}"
    shell: "{{ backuppc_client_item.shell }}"
    createhome: true
    state: "present"
    home: "{{ backuppc_client_item.home }}"
    comment: "{{ backuppc_client_item.comment }}"
    password: "*"
    password_lock: true
    update_password: always
  loop: "{{ backuppc_client }}"
  loop_control:
    loop_var: backuppc_client_item
  no_log: true
  changed_when: false
  when:
    - backuppc_client is defined
  notify: known_hosts
  tags: backuppc_client

# - name: Debug backuppc_client structure
#   debug:
#     var: backuppc_client

# - name: Set up backuppc user authorized_keys (only if backuppc_client is defined)
#   when:
#     - backuppc_client is defined
#     - backuppc_client | length > 0
#   block:
#     - name: Set up backuppc user authorized_keys
#       ansible.posix.authorized_key:
#         user: "{{ backuppc_client_item.0.username }}"
#         state: "present"
#         # key_options: "{{ backuppc_client_item.0.key_options | default(omit) }}"
#         key_options: "{{ backuppc_client_item.key_options | default(omit) }}"
#         # key: "{{ backuppc_client_item.1 }}"
#         key: "{{ backuppc_client_item.authorized_keys }}"
#         manage_dir: true
#       # loop: "{{ backuppc_client | subelements('authorized_keys') }}"
#       loop: "{{ backuppc_client }}"
#       loop_control:
#         loop_var: backuppc_client_item
#   tags: backuppc_client

# TODO: add hosts ssh key(s) to known_hosts file
# TODO: confirm host has a .pl file on the backuppc server

# Can override the location "{{ known_host_keys_dir }}"
# default is in defaults/main.yml
#
# - name: Fetch ssh public keys
#  fetch:
#    src: "/etc/ssh/{{ item }}"
#    dest: "{{ known_host_keys_dir }}/known_host_keys"
#  with_items:
#      - "{{ ssh_host_pub_keys }}"
#  ignore_errors: yes
#  register: ssh_host_keys
#  tags: backuppc_client
#
# - debug:
#    msg: "{{ ansible_fqdn }},{{ ansible_default_ipv4.address }} {{ lookup('file', item.dest).split(' ')[0:-1] | join(' ') }}"
#  when:
#    - not item.failed
#  with_items:
#    - "{{ ssh_host_keys.results }}"
#  ignore_errors: yes
#  tags: backuppc_client
#
# - name: Add ssh host key to backuppc known_hosts
#  known_hosts:
#    path: /etc/ssh/ssh_known_hosts
#    name: "{{ ansible_fqdn }}"
#    key: "{{ ansible_fqdn }},{{ ansible_default_ipv4.address }} {{ lookup('file', item.dest).split(' ')[0:-1] | join(' ') }}"
#    state: present
#  when:
#    - backuppc_server_hostname is defined
#    - not item.failed
#  with_items:
#    - "{{ ssh_host_keys.results }}"
#  delegate_to: "{{ backuppc_server_hostname }}"
#  remote_user: ansible
#  changed_when:
#    - item.changed
#  tags: backuppc_client

- name: Include tasks ssh_keys.yml
  ansible.builtin.include_tasks: ssh_keys.yml
  tags: backuppc_client

- name: Include tasks known_hosts.yml
  ansible.builtin.include_tasks: known_hosts.yml
  when: backuppc_ssh_key_scan is defined
  tags: backuppc_client
